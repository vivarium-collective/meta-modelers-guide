schema_version: 4
name: divide
investigation: draft-to-living-cell
title: Divide
created: '2026-08-15'
status: complete
phase: Decide
gate_status: passed
confidence: Accepted
question: Is cell division in Fig 10 a genuine structural rewrite of the place graph — a single cell node
  actually becoming two daughter nodes at runtime — rather than a pre-declared post-structure that is
  merely animated?
biological_summary: 'Fig 10 rewrites the cell across time, and division is its sharpest case. This is
  not a process nudging numbers in a fixed structure — it is a change to the place graph itself, a reaction
  rule in Milner''s sense whose reactum has more nodes than its redex. When a cell''s DNA crosses a
  threshold, the rule fires: the cell node is replaced by two daughter nodes, each inheriting a symmetric
  share of the parent''s DNA and mass (mother mass = Σ daughters). Fired repeatedly as the lineage grows
  on the shared nutrient (drawn 20 → 2.5), it takes cell_count from 1 to 14 over the run. The daughters
  are structurally new — they do not exist in the initial state — which is exactly what makes this an
  event-driven rewrite rather than an animation of a pre-built pair.'
claim: 'Division is a change to the composition itself: one cell becomes two through a graph rewrite that
  creates new nodes at runtime, not a pre-drawn pair.'
baseline:
- name: division-rewrite
  composite: viva_meta_modelers_guide.composites.fig10-1-rewrite
  params: {}
- name: division
  composite: viva_meta_modelers_guide.composites.fig10-1-division
  params: {}
variants: []
behavior_tests:
- name: division-occurs
  classification: primary
  measure:
    kind: observable
    path: cell_count
    expr: last(cell_count)
  pass_if:
    op: '>='
    value: 2.0
    provenance: lineage divides (exec cell_count 1→14)
  requires_simulation: division-occurs
- name: nutrient-consumed
  classification: primary
  measure:
    kind: observable
    path: nutrient
    expr: last(nutrient)
  pass_if:
    op: <=
    value: 5.0
    provenance: growing lineage draws nutrient down (exec last≈1.1 from 20)
  requires_simulation: nutrient-consumed
- name: draft-is-inert
  classification: diagnostic
  measure:
    kind: observable
    path: cell_count
    expr: last(cell_count)
  pass_if:
    op: '>='
    value: 2.0
    provenance: inert-draft run stays at seed (no mechanism → no dynamics)
  control: negative
  description: 'Negative control: the inert draft (no mechanism) MUST fail this.'
  requires_simulation: draft-is-inert
runs:
- name: divide
  composite: divide
  emitter: RAMEmitter
  id: divide
  status: completed
  provenance: scripts/render_dynamics.py — viva_meta_modelers_guide.dynamics builders run to completion
    through the engine; conservation invariant checked per figure.
  params: {}
  outcomes:
    DIVISION-FIRES:
      result: PASS
      detail: cell_count 1 → 14 over the run
    MASS-CONSERVED:
      result: PASS
      detail: mass sawtooth; halving conserves total
    AUTOCATALYTIC-GROWTH:
      result: PASS
      detail: nutrient 20→2.5 feeding growth
findings:
- id: F-01
  kind: biological
  tier: mechanism
  status: confirms
  statement: 'Growth to threshold fires repeated division events. Confirmed: cell_count 1 → 14 over the
    run.'
  evidence:
    from_test: division-fires
    from_run: divide
    observed: cell_count 1 → 14 over the run
- id: F-02
  kind: biological
  tier: mechanism
  status: confirms
  statement: 'Each division halves mass (mother = Σ daughters) — mass sawtooth. Confirmed: mass sawtooth;
    halving conserves total.'
  evidence:
    from_test: mass-conserved
    from_run: divide
    observed: mass sawtooth; halving conserves total
- id: F-03
  kind: biological
  tier: observation
  status: confirms
  statement: 'Between divisions mass grows autocatalytically on the nutrient. Confirmed: nutrient 20→2.5
    feeding growth.'
  evidence:
    from_test: autocatalytic-growth
    from_run: divide
    observed: nutrient 20→2.5 feeding growth
conclusion_verdicts:
  regression_compatibility:
    result: PASS
    basis: All figure composites build and every executable runs to completion (scripts/build_executables.py
      + scripts/measure_all.py, 2026-08-16).
  biological_validation:
    result: PASS
    basis: Division-fires, mass-conserved, and autocatalytic-growth all pass; the inert draft stays at cell_count 1, so division is a real runtime rewrite.
  explanatory_gain:
    result: POSITIVE
    basis: Watching one cell node become 14 via repeated mass-conserving rewrites makes division-as-graph-rewrite concrete rather than animated.
conclusion: '## Claims

  - Autocatalytic growth to a mass threshold triggers a MASS-CONSERVING division event (mother mass =
  Σ daughters); the lineage traces a mass sawtooth and cell_count 1→14 as it draws down the shared nutrient
  — a genuine event on top of continuous growth.


  ## Evidence

  - **DIVISION-FIRES** — cell_count 1 → 14 over the run

  - **MASS-CONSERVED** — mass sawtooth; halving conserves total

  - **AUTOCATALYTIC-GROWTH** — nutrient 20→2.5 feeding growth

  - **conservation** — divisions: cell_count 1→14; each division halves mass (mother = Σ daughters)


  ## Limitations

  - Toy-real division: each rewrite conserves mass, but the threshold and growth constants are illustrative and division is symmetric.


  ## Next steps

  - Add size-controlled or asymmetric division rules to test the rewrite mechanism under realistic division timing.'
conclusion_logic:
  if_primary_tests_pass:
    implementation_status: The figure draft compiles to an executable that runs.
    biological_validation: The paper's pattern is demonstrated at the toy-real (uncalibrated) level; interface
      preservation holds.
    pipeline_unblocks:
    - biofilm
  if_primary_tests_fail:
    diagnose:
    - compiler dropped/renamed an interface port (law 2 violation)
    - handler produced no non-trivial dynamics on the declared ports
    block_downstream: the-living-atlas (the composed whole cell depends on this figure's mechanism)
limitations:
- Symmetric division with a fixed DNA threshold; asymmetric division, septum placement, and size control are not modelled.
- The lineage is well-mixed on one shared nutrient — no spatial structure among the 14 cells.
falsifiability: Overturned if cell_count never increases (the rewrite never fires) or if a division fails to conserve mass (daughters' total ≠ mother's mass).
visualizations:
- name: divide-dynamics
  address: image:visualizations/divide-dynamics.svg
  config:
    chart: image
    caption: CLOSED-LOOP dynamics — conserved, run to completion.
- name: fig10-1-division
  address: image:visualizations/fig10-1-division.svg
  config:
    chart: image
- name: fig10-illustration
  address: image:visualizations/fig10-illustration.png
  config:
    chart: image
parent_studies:
- study: self-made
  condition: tests-passed
experiment: Install a Milner reaction rule behind the Fig 10 division interface and let the cell grow
  to threshold and fire it.
result: The single cell node is replaced by two daughters that conserve the parent's mass — a genuine
  structural rewrite, driven by the cell's own growth.
