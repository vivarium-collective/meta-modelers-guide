schema_version: 4
name: fig-09
title: Growth & division
kind: theoretical
purpose:
  mechanism: 'Cell growth and division as a place-graph rewrite: chromosome replication
    and segregation, then division into two daughter cells.'
visualizations:
- name: Figure 9
  address: svg:visualizations/figure_9.svg
  config:
    caption: 'Growth and division. (a) Prokaryotic cell division. (b) Division as a
      genuine runtime place-graph rewrite — one cell replicates its chromosome, then
      divides into two daughters; snapshots at t=0, t=1, t=2.'
- name: Division sequence
  address: image:visualizations/fig09b-division-sequence.png
  config:
    chart: image
    caption: 'Division as a runtime place-graph rewrite: one cell → chromosome replicates
      → cell divides. Snapshots at t=0, t=1, t=2 from the fig09-rewrite simulation.'
question: How is cell division a place-graph rewrite?
baseline:
- name: fig09-rewrite
  composite: meta_modelers_guide.composites.fig09-rewrite
  params: {}
