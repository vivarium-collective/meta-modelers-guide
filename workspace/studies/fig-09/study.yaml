schema_version: 4
name: fig-09
title: Growth & division
kind: theoretical
purpose:
  mechanism: 'Cell growth and division as a place-graph rewrite: chromosome replication
    and segregation, then division into two daughter cells.'
visualizations:
- name: Division sequence
  address: image:visualizations/fig09b-division-sequence.png
  config:
    chart: image
    caption: 'Division as a genuine runtime place-graph rewrite: the composition starts
      as one cell and, on running, rewrites its own topology — the chromosome replicates
      (1→2), then the cell divides (1→2 daughters). Three snapshots captured from running
      the fig09-rewrite simulation (t=0, t=1 cycle, t=2 cycles).'
question: How is cell division a place-graph rewrite?
baseline:
- name: fig09-rewrite
  composite: meta_modelers_guide.composites.fig09-rewrite
  params: {}
